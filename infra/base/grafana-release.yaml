apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: grafana
  namespace: default
spec:
  interval: 1m
  chart:
    spec:
      chart: grafana
      version: '6.0.0'
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: default
      interval: 1m
  values:
    replicaCount: 1
